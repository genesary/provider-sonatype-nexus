---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: iqserverconfigurations.instance.nexus.crossplane.io
spec:
  group: instance.nexus.crossplane.io
  names:
    kind: IQServerConfiguration
    listKind: IQServerConfigurationList
    plural: iqserverconfigurations
    singular: iqserverconfiguration
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IQServerConfiguration is the Schema for the IQ Server configuration
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IQServerConfigurationSpec defines the desired IQ Server state.
            properties:
              forProvider:
                description: ForProvider holds the provider-specific configuration
                  for this resource.
                properties:
                  authenticationType:
                    description: AuthenticationType is either "USER" or "PKI".
                    enum:
                    - USER
                    - PKI
                    type: string
                  enabled:
                    default: true
                    description: |-
                      Enabled indicates whether the IQ Server configuration is enabled or disabled.
                      If unset, will default to true.
                    type: boolean
                  failOpenModeEnabled:
                    default: false
                    description: |-
                      FailOpenModeEnabled indicates whether the fail open mode is enabled.
                      If unset, will default to false.
                    type: boolean
                  passwordSecretRef:
                    description: PasswordSecretRef references the Kubernetes Secret
                      key holding the IQ Server password.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                  properties:
                    description: Properties is a string of properties to set for the
                      IQ Server configuration.
                    type: string
                  showLink:
                    default: false
                    description: |-
                      ShowLink indicates whether to show the IQ Server link in the Nexus UI.
                      If unset, will default to false.
                    type: boolean
                  timeoutSeconds:
                    default: 10
                    description: |-
                      TimeoutSeconds is the timeout in seconds for the IQ Server connection.
                      If unset, will default to 10 seconds.
                    type: integer
                  url:
                    description: URL is the URL of the IQ Server.
                    type: string
                  useTrustStoreForUrl:
                    description: UseTrustStoreForURL indicates whether to use the
                      trust store for the IQ Server URL.
                    type: boolean
                  usernameSecretRef:
                    description: UsernameSecretRef references the Kubernetes Secret
                      key holding the IQ Server username.
                    properties:
                      key:
                        description: The key to select.
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                      namespace:
                        description: Namespace of the secret.
                        type: string
                    required:
                    - key
                    - name
                    - namespace
                    type: object
                required:
                - url
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: IQServerConfigurationStatus holds the observed IQ Server
              state.
            properties:
              atProvider:
                description: AtProvider holds the provider-specific observed state.
                properties:
                  authenticationType:
                    description: AuthenticationType is the observed authentication
                      type.
                    type: string
                  enabled:
                    description: Enabled reflects whether the IQ Server integration
                      is enabled.
                    type: boolean
                  failOpenModeEnabled:
                    description: FailOpenModeEnabled reflects whether fail open mode
                      is enabled.
                    type: boolean
                  properties:
                    description: Properties is the observed properties string.
                    type: string
                  showLink:
                    description: ShowLink reflects whether the IQ Server link is shown
                      in the Nexus UI.
                    type: boolean
                  timeoutSeconds:
                    description: TimeoutSeconds is the observed connection timeout
                      in seconds.
                    type: integer
                  url:
                    description: URL is the observed IQ Server URL.
                    type: string
                  useTrustStoreForUrl:
                    description: UseTrustStoreForURL reflects whether the trust store
                      is used for the IQ Server URL.
                    type: boolean
                required:
                - authenticationType
                - enabled
                - failOpenModeEnabled
                - properties
                - showLink
                - timeoutSeconds
                - url
                - useTrustStoreForUrl
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
