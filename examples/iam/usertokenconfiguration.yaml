apiVersion: iam.nexus.crossplane.io/v1alpha1
kind: UserTokenConfiguration
metadata:
  name: user-tokens
  namespace: default
spec:
  forProvider:
    enabled: true
    protectContent: false
    expirationEnabled: true
    expirationDays: 30
  providerConfigRef:
    kind: ProviderConfig
    name: default
